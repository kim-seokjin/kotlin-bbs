spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: board_user
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

springdoc:
  swagger-ui:
    enabled: false